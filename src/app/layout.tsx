import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";
import { ThemeProvider } from "@/components/theme-provider";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "TeamBrain — your firm's knowledge, one question away",
  description:
    "TeamBrain by INEHA TECH is a permission-aware AI knowledge copilot for small professional-services firms. Ask questions about your firm's documents and get cited answers that respect every user's Google Drive access.",
  keywords: ["TeamBrain", "INEHA TECH", "knowledge copilot", "RAG", "professional services", "AI search"],
  authors: [{ name: "INEHA TECH" }],
  icons: { icon: "/ineha-logo.png" },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        <ThemeProvider attribute="class" defaultTheme="light" enableSystem={false} disableTransitionOnChange>
          {children}
          <Toaster />
        </ThemeProvider>
      </body>
    </html>
  );
}
